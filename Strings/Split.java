package Strings;

public class Split {
    //String[] res=str.split('[/.]');
	//sop(res[res.length-1]); //lastword
	//sop(res[res.length-1].length());//last word length
	//sop(res[res.length-2]);//second last word
	//sop(res[res.length-2].length());//second last word length
}
